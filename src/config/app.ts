import packageJson from '../../package.json';

export const APP_ID = 'nomade-raiz';
export const APP_NAME = 'Nômade';
export const APP_VERSION = packageJson.version;
export const BACKUP_SCHEMA_VERSION = 1 as const;
